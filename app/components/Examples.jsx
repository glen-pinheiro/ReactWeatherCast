import React from 'react';

/*export default React.createClass({
  render: function() {
    return (
      <h1>Demo Page</h1>
    );
  }
});*/

var Examples = (props) => {
  return (
    <h1>Demos</h1>
  )
};

export default Examples;
